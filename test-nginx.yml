# Minimal test to verify port binding works
version: '3.8'

services:
  test-nginx:
    image: nginx:alpine
    container_name: test-nginx-spendly
    ports:
      - "8080:80"
    restart: unless-stopped
